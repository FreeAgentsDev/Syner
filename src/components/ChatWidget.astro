---
---

<div id="chat-widget" class="fixed bottom-6 right-6 z-50">
  <!-- Chat Button -->
  <button 
    id="chat-toggle"
    class="w-16 h-16 bg-gradient-to-br from-blue-600 to-cyan-600 hover:from-blue-500 hover:to-cyan-500 text-white rounded-full shadow-lg shadow-blue-500/50 flex items-center justify-center transition-all duration-300 hover:scale-110 active:scale-95"
    aria-label="Abrir chat"
  >
    <svg id="chat-icon" class="w-6 h-6 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
    </svg>
    <svg id="close-icon" class="w-6 h-6 hidden transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
    </svg>
  </button>
  
  <!-- Chat Window -->
  <div 
    id="chat-window"
    class="hidden absolute bottom-20 right-0 w-96 h-[600px] bg-gray-900 rounded-lg shadow-2xl flex flex-col border border-gray-800/50 backdrop-blur-sm animate-slide-up"
  >
    <!-- Chat Header -->
    <div class="bg-gradient-to-r from-blue-600 to-cyan-600 text-white p-4 rounded-t-lg flex items-center justify-between">
      <div class="flex items-center space-x-3">
        <div class="w-10 h-10 bg-white/20 rounded-full flex items-center justify-center">
          <span class="font-bold">S</span>
        </div>
        <div>
          <h3 class="font-semibold">Synergrowth</h3>
          <p class="text-xs text-blue-100">En línea</p>
        </div>
      </div>
    </div>
    
    <!-- Chat Messages -->
    <div id="chat-messages" class="flex-1 overflow-y-auto p-4 space-y-4 bg-gray-950">
      <div class="flex items-start space-x-2">
        <div class="w-8 h-8 bg-gradient-to-br from-blue-600 to-cyan-600 rounded-full flex items-center justify-center flex-shrink-0">
          <span class="text-white text-xs font-bold">S</span>
        </div>
        <div class="bg-gray-800/50 rounded-lg p-3 shadow-sm max-w-[80%] border border-gray-700/50">
          <p class="text-sm text-gray-300">
            ¡Hola! 👋 Bienvenido a SynerGrowth. ¿En qué puedo ayudarte hoy? Estoy aquí para responder tus preguntas sobre nuestros servicios de publicidad digital y generación de leads para PYMEs del sector de sistemas.
          </p>
        </div>
      </div>
    </div>
    
    <!-- Chat Input -->
    <div class="p-4 border-t border-gray-800/50 bg-gray-900 rounded-b-lg">
      <form id="chat-form" class="flex space-x-2">
        <input
          type="text"
          id="chat-input"
          placeholder="Escribe tu mensaje..."
          class="flex-1 px-4 py-2 bg-gray-800/50 border border-gray-700/50 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent text-white placeholder-gray-500 transition-all"
          autocomplete="off"
        />
        <button
          type="submit"
          class="bg-gradient-to-r from-blue-600 to-cyan-600 hover:from-blue-500 hover:to-cyan-500 text-white px-4 py-2 rounded-lg transition-all duration-300 hover:scale-105"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" />
          </svg>
        </button>
      </form>
      <p class="text-xs text-gray-500 mt-2 text-center">
        Respuestas automatizadas con IA • Se conectará a Gemini próximamente
      </p>
    </div>
  </div>
</div>

<style>
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-slide-up {
    animation: slideUp 0.3s ease-out;
  }

  /* Custom scrollbar for chat */
  #chat-messages {
    scrollbar-width: thin;
    scrollbar-color: rgba(59, 130, 246, 0.5) transparent;
  }

  #chat-messages::-webkit-scrollbar {
    width: 6px;
  }

  #chat-messages::-webkit-scrollbar-track {
    background: transparent;
  }

  #chat-messages::-webkit-scrollbar-thumb {
    background-color: rgba(59, 130, 246, 0.5);
    border-radius: 3px;
  }

  #chat-messages::-webkit-scrollbar-thumb:hover {
    background-color: rgba(59, 130, 246, 0.7);
  }
</style>

<script>
  // Chat Widget Functionality
  const chatToggle = document.getElementById('chat-toggle');
  const chatWindow = document.getElementById('chat-window');
  const chatIcon = document.getElementById('chat-icon');
  const closeIcon = document.getElementById('close-icon');
  const chatForm = document.getElementById('chat-form');
  const chatInput = document.getElementById('chat-input');
  const chatMessages = document.getElementById('chat-messages');
  
  let isOpen = false;
  
  // Toggle chat window
  if (chatToggle && chatWindow) {
    chatToggle.addEventListener('click', () => {
      isOpen = !isOpen;
      chatWindow.classList.toggle('hidden');
      chatIcon.classList.toggle('hidden');
      closeIcon.classList.toggle('hidden');
      
      if (isOpen && chatInput) {
        setTimeout(() => chatInput.focus(), 100);
      }
    });
  }
  
  // Handle form submission
  if (chatForm && chatInput && chatMessages) {
    chatForm.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const message = chatInput.value.trim();
      if (!message) return;
      
      // Add user message
      addMessage(message, 'user');
      chatInput.value = '';
      
      // Show typing indicator
      const typingId = showTypingIndicator();
      
      // TODO: Connect to Gemini API here
      // For now, simulate a response
      setTimeout(() => {
        removeTypingIndicator(typingId);
        // Simulated response - replace with Gemini API call
        const response = getSimulatedResponse(message);
        addMessage(response, 'bot');
      }, 1000);
    });
  }
  
  function addMessage(text: string, sender: 'user' | 'bot') {
    if (!chatMessages) return;
    
    const messageDiv = document.createElement('div');
    messageDiv.className = `flex items-start space-x-2 ${sender === 'user' ? 'flex-row-reverse space-x-reverse' : ''}`;
    
    const avatar = document.createElement('div');
    avatar.className = `w-8 h-8 rounded-full flex items-center justify-center flex-shrink-0 ${
      sender === 'user' 
        ? 'bg-gray-600' 
        : 'bg-gradient-to-br from-blue-600 to-cyan-600'
    }`;
    avatar.innerHTML = sender === 'user' ? '<span class="text-white text-xs">Tú</span>' : '<span class="text-white text-xs font-bold">S</span>';
    
    const messageContent = document.createElement('div');
    messageContent.className = `rounded-lg p-3 shadow-sm max-w-[80%] border ${
      sender === 'user'
        ? 'bg-gradient-to-r from-blue-600 to-cyan-600 text-white border-transparent'
        : 'bg-gray-800/50 text-gray-300 border-gray-700/50'
    }`;
    messageContent.innerHTML = `<p class="text-sm">${escapeHtml(text)}</p>`;
    
    messageDiv.appendChild(avatar);
    messageDiv.appendChild(messageContent);
    chatMessages.appendChild(messageDiv);
    
    // Scroll to bottom with smooth animation
    chatMessages.scrollTo({
      top: chatMessages.scrollHeight,
      behavior: 'smooth'
    });
  }
  
  function showTypingIndicator(): string {
    if (!chatMessages) return '';
    
    const typingId = 'typing-' + Date.now();
    const typingDiv = document.createElement('div');
    typingDiv.id = typingId;
    typingDiv.className = 'flex items-start space-x-2';
    
    const avatar = document.createElement('div');
    avatar.className = 'w-8 h-8 bg-gradient-to-br from-blue-600 to-cyan-600 rounded-full flex items-center justify-center flex-shrink-0';
    avatar.innerHTML = '<span class="text-white text-xs font-bold">S</span>';
    
    const typingContent = document.createElement('div');
    typingContent.className = 'bg-gray-800/50 rounded-lg p-3 shadow-sm border border-gray-700/50';
    typingContent.innerHTML = '<div class="flex space-x-1"><div class="w-2 h-2 bg-gray-400 rounded-full animate-bounce"></div><div class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 0.1s"></div><div class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 0.2s"></div></div>';
    
    typingDiv.appendChild(avatar);
    typingDiv.appendChild(typingContent);
    chatMessages.appendChild(typingDiv);
    
    chatMessages.scrollTo({
      top: chatMessages.scrollHeight,
      behavior: 'smooth'
    });
    
    return typingId;
  }
  
  function removeTypingIndicator(id: string) {
    const indicator = document.getElementById(id);
    if (indicator) {
      indicator.remove();
    }
  }
  
  function getSimulatedResponse(message: string): string {
    const lowerMessage = message.toLowerCase();
    
    if (lowerMessage.includes('servicio') || lowerMessage.includes('qué ofrecen') || lowerMessage.includes('que hacen')) {
      return 'Ofrecemos publicidad digital, generación de leads cualificados y optimización de procesos de venta para PYMEs del sector de sistemas. Trabajamos con técnicas avanzadas de marketing digital para aumentar tu visibilidad y obtener más llamadas de venta. ¿Te gustaría conocer más detalles?';
    } else if (lowerMessage.includes('precio') || lowerMessage.includes('costo') || lowerMessage.includes('cuánto') || lowerMessage.includes('precio')) {
      return 'Nuestros servicios están diseñados para generar resultados tangibles. Ofrecemos una garantía de devolución de dinero si no cumplimos con los objetivos propuestos. ¿Podrías contarme más sobre tu PYME para darte una propuesta personalizada?';
    } else if (lowerMessage.includes('garantía') || lowerMessage.includes('garantia')) {
      return 'Sí, ofrecemos una garantía de devolución del dinero si no logramos cumplir con los objetivos propuestos. Tu éxito es nuestra prioridad. ¿Te gustaría conocer más sobre cómo trabajamos?';
    } else if (lowerMessage.includes('leads') || lowerMessage.includes('clientes')) {
      return 'Nos especializamos en generar leads cualificados y llamadas de venta para PYMEs del sector de sistemas. Utilizamos técnicas avanzadas de marketing digital para conectar a nuestros clientes con personas realmente interesadas en sus productos o servicios. ¿Quieres saber más?';
    } else if (lowerMessage.includes('contacto') || lowerMessage.includes('hablar') || lowerMessage.includes('reunión') || lowerMessage.includes('reunion')) {
      return '¡Perfecto! Puedes contactarnos a través de WhatsApp o LinkedIn. También podemos agendar una consulta gratuita para discutir cómo podemos ayudar a tu PYME a crecer. ¿Cuál prefieres?';
    } else {
      return 'Gracias por tu mensaje. Estoy aquí para ayudarte con cualquier pregunta sobre SynerGrowth y nuestros servicios de publicidad digital y generación de leads para PYMEs del sector de sistemas. ¿Hay algo específico en lo que pueda asistirte?';
    }
  }
  
  function escapeHtml(text: string): string {
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
  }
</script>

<style>
  @keyframes bounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-4px);
    }
  }
  
  .animate-bounce {
    animation: bounce 1s infinite;
  }
</style>
